import styled from './showTwoTeams.module.scss';

interface ShowTwoTeamsProps {}

export const ShowTwoTeams = ({}: ShowTwoTeamsProps)=>{
  return (
    <div id={styled["showTwoTeams"]}>
    </div>
  );
} 