import React from 'react';
import { render } from '@testing-library/react';
import { ShowMembersOfTeams } from './';
 
test('render component correctly', () => {
  render(<ShowMembersOfTeams />);
});