import styled from './styled.module.scss';

interface SituationPartidaProps {}

export const SituationPartida = ({}: SituationPartidaProps) => {
  return <div id={styled['situationPartida']}></div>;
};
